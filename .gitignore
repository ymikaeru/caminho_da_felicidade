# ── Dependências e ferramentas ──
node_modules/
__pycache__/
*.pyc
.agent/
.claude
.cli-latest

# ── Segredos — NUNCA commitar ──
.env
.env.*
.env.local
auth-config.json

# ── Backups e arquivos temporários ──
# Backup/ = arquivo morto local (snapshots, protótipos, notas)
# backups/ = dumps do banco (scripts/backup-db.ps1) — dados sensíveis
Backup/
backups/
*.bak
*.bkp
*.tmp
*.sql.gz
*.sql.zip
backup-*.sql

# ── Dados de processamento local — não vão ao repo ──
scripts/storage_update/
scripts/storage_update2/
scripts/storage_update3/
scripts/retrad_staging/
supabase/.temp/

# Espelho local do Storage para edição em massa (storage-pull / push)
.local-edits/

# Fontes geradas para o NotebookLM (regeneráveis: python scripts/export_notebooklm.py)
notebooklm_export/

# Cópia local de livros p/ revisar deslogado (fallback site_data) — NÃO publicar
site_data/books/
