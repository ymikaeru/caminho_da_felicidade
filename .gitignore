node_modules/
.env
.env.local
.env.*
auth-config.json
*.pyc
__pycache__/
.agent/
.claude

# Dados de processamento local — não vão ao repo
scripts/storage_update/
scripts/storage_update2/
scripts/storage_update3/
supabase/.temp/

# Espelho local do Storage para edição em massa (storage-pull / push)
.local-edits/

# Cópia local de livros p/ revisar deslogado (fallback site_data) — NÃO publicar
site_data/books/

# Backups do banco — contêm dados sensíveis, nunca commitar
backups/
*.sql.gz
*.sql.zip
backup-*.sql

.env.local
ComandosUteis
.cli-latest
supabase/.temp

# Backups e arquivos temporários gerados pelo translate_akimaro_gemini.py
*.json.bak
*.json.tmp
# Arquivos organizacionais — backups locais, scripts one-shot ja executados
Backup/

# Staging da retraducao em lote (617 paredoes) — undo vive no content_ptbr_prev (Storage) + disco local
scripts/retrad_staging/
